from utilities.fighting_strategies import SmarterBattleStrategy


class CanopusBattleStrategy(SmarterBattleStrategy):
    """Canopus card policy; inherits the general policy until custom rules are needed."""

